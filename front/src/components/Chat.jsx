import React from 'react';
import Box from '@mui/material/Box';
import TextField from '@mui/material/TextField';

import Base from '../Base';

class Chat extends Base
{
    state = {
    };

    render = () => {
        return (
            <Box>
            </Box>
        );
    };
}

export default Chat.export;